import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="sticky top-0 z-50 bg-slate-100">
      <div className="max-w-7xl mx-auto px-4">header</div>
    </header>
  );
};

export default Header;
